name: workflow for jenkins trigger

on:
  push:
    branches:
    - stg
    
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: trigger Job
      uses: appleboy/jenkins-action@master
      with:
        url: "https://jenkins.quinnox.info"
        user: "admin"
        token: "1180651654575c70df7a3174d0716be6fe"
        job: "stg-githubactions-job"
