name: workflow for assign

on:
  pull_request:
    branches:
    - dev
jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: delivery-much/actions-assigner@v1
        with:
          token: ${{ secrets.GH_TOKEN }}
          reviewers: NiranjanRS
